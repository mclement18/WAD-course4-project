Tip.removeFromPage('<%= @tip.id %>', `<%= raw(t('account.tips.index.no_tips')) %>`);

<% if flash.any? %>
  <% flash.each do |name, message| %>
    Alert.render('<%= message %>', '<%= name %>', '<%= alert_id %>');
  <% end %>
<% end %>
